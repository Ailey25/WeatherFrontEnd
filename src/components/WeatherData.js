import React from "react";
import ReactDOM from "react-dom";

class WeatherData extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <label>Single weather data</label>
    );
  }
}

export default WeatherData;
